Frontend of the application; it's very limited at this point, and makes use of default CSS for styling, rathering than TailwindCSS or more powerful styling libraries.

The components are built with React and Typescript, and are intended to be minimalist for the sake of representating the data transfer from DB to client/user.